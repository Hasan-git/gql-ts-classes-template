{{ toComment description }}
export class {{@root.config.interfacePrefix}}{{ toPascalCase name }}Base{{#if hasInterfaces}} extends {{#each interfaces}}{{@root.config.interfacePrefix}}{{ toPascalCase this }}{{#unless @last}},{{/unless}}{{/each}}{{/if}} {
  constructor (
    public __typename: string =  "{{ toPascalCase name }}",
    {{#each fields}}
      public {{ name }}: {{ convertedType this }} = {{ constructType this }}, 
    {{/each}}
  ){}
}